import { ResetPasswordPage } from '@/components/auth/ResetPasswordPage';

export const metadata = {
  title: 'Set new password — Binee',
};

export default function ResetPassword() {
  return <ResetPasswordPage />;
}
